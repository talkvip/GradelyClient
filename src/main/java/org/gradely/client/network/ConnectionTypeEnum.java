package org.gradely.client.network;

/**
 * Used with network function to determine what type of connection to use for a given upload.
 * GET and POST are http types.
 * @author Matt
 */
public enum ConnectionTypeEnum {
    GET,
    POST
    
}
